# NEXT.md — 当前站在哪、下一步做什么

> **这个文件是覆盖式的，不是追加式的。** 做完一项就改掉它，不留历史。
> 历史属于 `memory/learning-log.md`，事实属于 `memory/`，**这里只有"现在"**。
> 存在理由见 `memory/pitfalls.md`「学习路线的"下一步"躺了两个月没被执行」（2026-09-14）。

**最后更新：2026-09-14**

---

## 下一个具体动作

**给 `samples/tool-call-loop` 接一个真实模型后端，跑 `--all`。**

代码已写完并验收（11 测试绿 + CLI 对本地假服务器端到端跑通），但**一次真实模型都没跑过**——
"真实模型会不会选对工具"目前零证据，这是阶段一唯一剩下的缺口。

- 卡点：本机没有可用后端。**GitHub Models 已退役**（2026-09-14 实测 410 Gone）；
  **Copilot 订阅不提供第三方程序可用的推理 API**。
- 二选一（都行，挑便宜的）：
  1. 本机装 Ollama + 一个支持 tool calling 的模型 → `LLM_BASE_URL=http://localhost:11434/v1`
  2. 根目录 `.env` 填 DeepSeek key → `LLM_BASE_URL=https://api.deepseek.com/v1`
- 跑法：`cd samples/tool-call-loop && PYTHONPATH=src .venv/bin/python -m toolloop --all`
- **要看轨迹，不只看 PASS/FAIL**：`count_lines` 有没有先 list 再 read、有没有漏排除 `.md`、
  有没有在 `max_steps` 上撞墙。

## 路线图阶段完成状态

对照 `docs/roadmap.md`。**跳过的阶段要留下痕迹，否则下次又会假装它做过了。**

| 阶段 | 主题 | 状态 | 证据 / 备注 |
|---|---|---|---|
| 一 | LLM 与 Agent 地基（tool calling 最小循环） | 🟡 **代码完成，未接真实模型** | `samples/tool-call-loop/`：沙箱 + 3 工具 + 循环 + 轨迹 + 3 个终态断言任务，11 测试绿。**缺口：真实模型零次运行** |
| 二 | Agent 核心范式（ReAct、循环控制） | ⬜ **未做** | — |
| 三 | 上下文与记忆（RAG） | 🟡 部分 | 做过字面 vs 向量受控对比（`cn-curriculum-graph/docs/rag-vs-literal.md`），但没做过 agent 侧的记忆 |
| 四 | 主流框架（LangGraph） | ✅ 已验收 | 2026-07-27 手写 vs 框架受控实验。**注意：验收的是"用图描述流水线"，不是 agent** |
| 五 | 工具、协议与集成（MCP） | 🟡 部分 | 写过 MCP server（`cn-curriculum-graph/serve/`），但没写过**消费**工具的一侧 |
| 六 | 多 Agent 系统 | ⬜ 未做 | 有多 agent **开发**经验（用 agent 写代码），不是多 agent **系统** |
| 七 | 评估、可观测与质量 | 🟢 **强项** | 判据校准、可复现性验证、工作点纪律。**但只做过结果评估，没做过轨迹评估** |
| 八 | 生产化（可靠性、安全） | ⬜ 未做 | — |

**结构性判断**：能力分布曾是"七强、四中、一二空"。阶段一的地基已经浇上（代码层面），**但没通电** —— 接真实后端之前不许往阶段二走。

## 之后的顺序（定了方向，未定细节）

1. 阶段二：扩成 ReAct 多轮。重点是**让它失败**——去掉 `max_steps` 看死循环、喂垃圾工具返回看它被带偏
2. **轨迹评估**（接强项，差异化所在）：结果对但路径错 / 结果错但路径对
3. 安全：沙箱、工具权限最小化、步数与预算上限、注入恶意工具返回

## 硬边界

- **不许改 `practice/cn-curriculum-graph/` 的代码。** 那个项目已结案。要用它只能 import。
- **不许在阶段一没接真实模型之前开阶段二。** 「测试全绿」不等于「跑通了」—— 11 个测试全部跑在假模型上，它们证明的是**代码对**，不是**agent 行**。
- 一旦发现自己在优化旧流水线，**停下来看这一行**。
