# 学习日志 (learning-log)

按时间倒序记录学习进度。每条：日期 · 主题 · 关键收获 · 产出物（sample/notes 链接）。

格式：
```
## YYYY-MM-DD — <主题>
- 收获：<1-3 条核心要点>
- 产出：<samples/xxx 或 docs/xxx 或 无>
- 下一步：<可选>
```

---

## 2026-07-29 — 带全部修复的第一次全量重跑：图全面变好，而"变好了多少"这句话大部分不成立

- **一句话**：注册表 + 空返回重试上身后完整跑完六层（约 1000 次调用 / 11 分钟），出了一张 **78 节点 / 213 边 / 孤立率 5% / 最长链 12** 的新图（基线是 66 / 162 / 11% / 10），`data/topic-registry.json` 第一次落盘。但本轮真正的收获是**三次差点把好看的数字写进文档**，三次都被自己拦下来了。

- **① 开跑前撞见的缺口：注册表只接在手写引擎上。** `graph.node_assemble`（两个 LangGraph 引擎共用）调 `assemble` 时没传 registry，静默退回 `sha1(name)` —— 而生产上跑的正是 fanout。**它躲过了那条专为对等性存在的逐字节跨引擎比对**，因为空注册表首轮认领不到条目、两条路径 id 恰好相同；差异要到第二轮才现形。补的测试是**跑两轮 + 中间同义改写 + 断言 id 不变**，三个引擎各一份，RED 时手写版过、两个 LangGraph 引擎全挂。**规则：跨实现对等性测试的射程止于它跑的轮数；状态跨运行累积的功能必须跑两轮才谈得上验证。**

- **② 差点做成的一件错事：种子化注册表。** 重跑后 17 条评测样本死了 8 条锚点。想用旧图种子化注册表把身份继承过来 —— 离线干跑显示标签存活 9/17 → **14/17**。逐条看认领明细才发现，多出来的 5 条里有 3 条来自一个假合并：**「小数的意义」→「万以内数的意义」，合成分 0.604，只高出门槛 0.004**，那三条小数样本会被挂到一个讲整数的节点上。**"只看存活率会选出错误的方案"是上一轮刚写下的教训，换个场景立刻又踩一次** —— 教训写进文档不会在下一个决策点自动触发，触发它的是"每次都把明细逐条打出来看"。副产物：`CLAIM_THRESHOLD=0.6` 的"0 假合并"是在三次**同管道**运行上量的，跨越 66→78 这种更大的名称集变动就出现 2/45 假合并，**而且没有余量**（假合并 0.604 排在真认领 0.696/0.844 下面却仍过线）。

- **③ 差点写下的第三个错数：recall 从 79% 掉到 65%。** 先去复现 79% —— **在 07-28 那张 66 节点图上根本复现不出来，只有 59%**。查清楚了：79% 绑定在 **07-27 的 64 节点图**上，而 learning-log 里记着"重跑后掉了 17 个百分点、那个数字不可信"（79−17≈62，实测 59）。三张图串起来是 79%（64 节点）→ 59%（66 节点）→ 65%（78 节点），**跨图比 recall 根本不成立**，分母、样本、标签全在变。要比就限定到两轮都存在的同一批 15 条观察句：**53% → 67%**，逐条配对是**两边都中 7｜新图独赢 3｜基线独赢 1｜都不中 4**，4 对不一致的符号检验 **p≈0.63 —— 与噪声区分不开**。而且新图独赢那三条赢的原因不是检索变强，是**新图里终于有了对得上的节点**（数感的进一步发展、奇数与偶数都回来了）。

- **④ 标签重挂的两条纪律。** 依据一律是"新图哪个节点的**误概念原文**对得上"，**不按检索排序挑**（按排序挑会挑到字面像但讲的不是一回事的节点）。两条样本在新图里**没有任何节点带那条误概念**（`dec_trailing_zero` 末尾零、`subtraction_commutes` 减法交换）—— 留着等于让检索指标永久背一个没人能答对的锅，删掉又正好删掉新图最差的两条。折中是移出计召回集但**覆盖缺口单独记账**。反过来，07-28 因同样理由删掉的两条**按当时写下的条件加了回来**（节点回来了、误概念对得上），**加回来把 recall@3 从 67% 拉到 65% —— 会拉低分数不是不加的理由**。其中 `比的含义` 的 id 与删除前完全相同（名称/领域/起始学段没变，sha1 自然一致）。

- **⑤ 一个操作层面的坑**：`io.append_drops` 是**读已有文件再累加**的，跑进上一轮的产出目录会静默继承 194 条旧 drop 记录，记账立刻不可信。新一轮必须跑进空目录（旧产出先挪走留作基线）。同理，`--checkpoint` 复用同一个文件 + 同样的 `--source/--out` 会派生出同一个 thread_id，**直接"续跑"到已完成状态、一次调用都不发**。

- 产出：`data/generated/`（新图）、`data/topic-registry.json`（**入库**）、`pipeline/graph.py` 注册表接线、`tests/pipeline/test_run.py` 六条新测试（三引擎 × 两条）、`docs/pipeline-reproducibility.md` 末节、`docs/rag-vs-literal-backlog.md` 两节新开、`data/diagnosis-eval-groundtruth.json` 全量重挂、README/mcp-server-design/rag-vs-literal 三处口径补注、`memory/pitfalls.md` 两条。**416 测试全绿。**

- **下一步**：
  1. **注册表的红利下一轮才开始兑现**（本轮空表起步，认领不到任何旧身份）。下次重跑时该量的是：id Jaccard 是不是接近验收时离线复算的 70%，以及**评测标签这次能活下来几条** —— 这是注册表第一次面对真实考验。
  2. **recall@3 = 65% 仍低于 75% 闸门**，`scripts/eval_diagnosis.py` 非零退出。5 条未命中里 4 条是**空手而归**（字面分够不到 0.25），且集中在"图里写字母/抽象规则、家长说具体数字"这一类（`distributive_partial` 的图侧原文是 `(a+b)×c = a×c+b`）。**这不是调阈值能解决的**，前沿显示 0.15 能到 80% 但空样本正确率掉到 73%。
  3. **误概念的内容抖动没量过**：`加法交换律` 节点这轮还在，但丢了「对减法也成立」那条 —— **节点在不等于误概念在**。注册表只稳身份不稳内容，这一维至今没有数据。
  4. `docs/mcp-server-design.md` §8.1 的调用实录已补注"绑定 64 节点图"，但**没有在新图上重录一遍** —— 那需要真起 MCP server 走一轮对话。

## 2026-07-28（第二段）— 从「31 条边太少」一路挖到「流水线不可复现」：五次怀疑，五次都不是最初那个原因

- **一句话**：本来只想查"64 节点为什么只有 31 条边"，结果挖出四层，每层的真因都和第一反应不同。修完边 31→162、孤立率 41%→11%、最长链 3→10 层，规划线第一次能用；但**同时发现整条流水线不可复现**，这件事比边多不多大得多。

- **① 边少不是"边质量差"，是审核层没拿到前置知识点。** `review.py` 的边审 prompt 给了目标的 name+description，前置**只给了一个不透明的 draft_id 字符串** —— 让模型判断"A 是不是 B 的前置"，却只告诉它 B 是什么。345 条候选活下来 31 条（12%）。
  - **诊断链条值得复用**：先看 dropped 原因分布（发现 246 条 `REVIEW_REJECTED` 里混着 8 条 topic 和 238 条边，只能靠 ref 有没有 `<-` 区分 —— 这个记账缺陷让我第一眼判错了主因）；再**逐票统计而不是逐结论统计**（`pitfalls.md` 那条教训）；发现 flash 在 topic 的 fidelity 上通过率 99%、在边上塌到 12%，而 pro 是 49%，**分歧方向 102:1 一边倒**。不是"档位不够"（那种是方向随机），是系统性的。最后读 flash 的否决理由：**65% 直接写着"未提供该知识点的具体名称和内容，无法判断"**。
  - **最值钱的一点**：pro 之所以还能表态，是**从边自带的 reason 里反推前置是谁** —— 拿待验证的说法去认定待验证的对象。所以 flash 的 12% 才是诚实的，pro 的 49% 是循环论证。**"更强的模型给出更高通过率"在这里恰恰是坏消息。**
  - 30 条边的小样本 A/B（同一批边、同模型、temperature=0，只动 prompt）：flash 17%→60%，"无法判断"类否决 60%→**0%**，pro 57%→53% 基本不变。**只有信息缺失的那一方被修好，这正是"输入 bug"该有的形状。**

- **② 重跑后检索掉 17 个百分点 —— 但那个数字不可信。** 同一份课标原文、同一套代码、四层全 `temperature=0`，跑三次得到三张对不上的图，**节点名两两 Jaccard 只有 29%~52%**。所以"边好了、检索差了"这个取舍根本说不清有多少是改动、多少是抽签。
  - 动荡分两块：**28 个 chunk 里 8 个（29%）至少一次整块颗粒无收**，每次是不同的几块；以及**"消失"的名称里 64%~80% 其实是同义改写**（"计数单位的感悟"→"计数单位"、"了解十进制计数法"→"十进制计数法"）。**内容是稳的，抖的是措辞。**
  - **射程要划清楚**：字面 vs 向量那轮**不受影响** —— 它把图固定住只换打分器，是干净的单变量对比。被推翻的只是**跨重跑**的指标比较。
  - **可推广的规则**：**任何"改了 X 之后指标变化 Y"的结论，都欠一个前提 —— 这条流水线跑两次给出同一个结果吗？** 这个前提直到这天才第一次被验证，而它不成立。

- **③ 29% 的 chunk 无产出：不是模型不行，是逃生舱被误用。** 排除法全程用数据：不是长度（不稳的中位 42 字、稳的 40 字，范围重叠）；不是调用失败（9 条 `NO_DRAFTS` vs 1 条 `EXTRACT_FAILED`）；**不是模型不够强 —— flash 与 pro 的空返回率没有差别，pro 在 #004 上还更高（2/5 vs 1/5）**。根因是 prompt 里"如果拆不出任何可教的知识点，返回空列表"被用在了明显可教的内容上。
  - 修法是重试。**重试不是"逼模型硬凑"的依据**：28 条素材逐条读过，全是实打实的课标条目，没有一条拆不出知识点，所以空返回一律是误判。仍设上限、全空照常记账并写明试了几次。
  - **一个被证伪的怀疑**：加完重试第一轮 #004 连空 3 次，怀疑是相同请求撞上服务端缓存。用条件概率判定（若粘滞则 `P(空|上一次空)` 应≈100%）：实测 **0/2**。而"连空 3 次"根本不需要解释 —— 基础率 20% 下单 chunk 连空三次是 0.8%，**28 个里至少出现一个的概率是 20%**。我把一个普通随机事件当成了异常信号。

- **④ 节点身份：`sha1(name)` 把措辞抖动放大成了身份变更。** 名字删两个字 id 就变，24 个评测标签一次重跑死了 12 个。四个方案在三次真实运行上量（判据="标签能否活过重跑"，不循环）：现状 58%／归一化名称 58%／**`chunk_id`+序号 92% 但有 5 个假合并**／相似度认领 83% 且 0 假合并。
  - **选了 83% 那个，因为代价不对称**：id 变动至少会被发现（标签失效、评测拒跑），**假合并是静默的错误身份**，下游拿去做规划不报错、只算错。**只看存活率会选出错误的方案。**
  - 中途我报过一次错数：原型的 83% 是因为**漏了 domain/grade 约束**，真实现只有 62%。查下去发现更根本的问题 —— 我一直用的"名称相似度≥0.6 = 同一知识点"这个判据**本身有假阳性**（"数位的含义"vs"比的含义"靠"的含义"就能凑到 0.6）。补救不是放宽约束，是**换更准的判据**：学段改用区间重叠（`grade_start` 自己也在抖，同一知识点跨轮只有 78% 保持起点）、认领得分改成 `0.7×名称 + 0.3×描述`。唯一被描述救回来的是「质数（素数）和合数」→「质数与合数」（名称 0.50 掉出门槛、描述 0.91）。
  - **代价没藏**：图不再是源的纯函数，性质变成「同一份源 **+ 同一份注册表** → 同一张图」。所以注册表入库（`data/topic-registry.json`）—— 按 ADR-0005，不入库的状态在两机交替的工作流里等于不存在。

- **⑤ 方法论（这一段真正的收获）**：五次追查，**没有一次是最初怀疑的那个原因**。边少不是边差、掉分不是改动、无产出不是模型弱、连空不是缓存、身份变不是内容变。共同点是每次都**先量再判** —— 逐票统计、条件概率、排除法、apples-to-apples 对照、候选方案离线回放。反过来说：**如果这五次里有任何一次直接按第一反应动手（改 prompt、换模型、加重试次数、回滚图），都会修错地方，而且会留下一个"看起来修好了"的假象。**

- 产出：`docs/pipeline-reproducibility.md`（三次运行的完整量化 + 四方案对比）、`pipeline/topic_registry.py`（稳定 id 注册表）、`pipeline/extract.py` 空返回重试、`pipeline/review.py` 边审输入修复、`scripts/eval_diagnosis.py` 标签失效守卫、`memory/pitfalls.md` 一条。410 测试全绿。

- **下一步**：
  1. ~~**验收还没做**~~ **[2026-07-29 已做，通过]**：只跑抽取层三次（约 110 次调用，不必花 3000 次跑完整流水线 —— 注册表的效果可以离线复算）。**chunk 覆盖 24/23/26 → 26/27/28；节点名 Jaccard 中位 32% → 57%；id Jaccard 中位 42%（旧方案）→ 70%（注册表）。** 两处修复的贡献可分离：重试补回整块缺失的 chunk（抬名称 Jaccard），注册表吸收同义改写（在此基础上再抬 28pp）。**判据是跑之前写死的**，且实测 57% 落在事先划的区间内（若涨到 80%+ 反而说明成分拆分判断错了）。混淆因素也查了：B-C 同一天只有 29%、r1-r2 跨天有 67%，时间接近解释不了。**仍未解决**：三成 id 活不过重跑；命名措辞抖动一点没修（名称 Jaccard 仅 57%），注册表只是让它不再传导到身份上。详见 `docs/pipeline-reproducibility.md` 末节。
  2. ~~本地 `data/generated/` 仍是修复前的产出，`data/topic-registry.json` 尚未生成（跑一次流水线才会创建）。~~ **[2026-07-29 已做]** 全量重跑完成，注册表已入库；开跑前还发现注册表**只接在手写引擎上**，两个 LangGraph 引擎静默退回 `sha1(name)` —— 见本文件 2026-07-29 那条。
  3. **A→C 只有 46%**：注册表补不了"整块 chunk 换一批知识点"那种动荡，那是抽取层切分粒度的问题（同一 chunk 这次出 6 个、下次出 5 个），还没碰。

## 2026-07-28 — 空样本 3 → 11 条重扫：这次被推翻的不是结论，是裁判

- **一句话**：把诊断评测的"应当召回不到"样本从 3 条补到 11 条，重扫字面与向量两条前沿。**结论方向没变（向量仍然没赢），但所有基线数字都变了，而且差距变大了** —— 同工作点（空样本正确率 = 100%）从「字面 89% @0.15 vs 向量 79% @0.7」变成 **「字面 79% @0.25 vs 向量 53% @0.75」**（10pp/2 条 → 26pp/5 条）。**不改任何代码**，`LiteralScorer.min_relevance` 保持 0.2，396 测试全绿。
- **① ⭐ 本轮唯一真正值钱的东西：判据本身太脆 —— 那个判据的分母只有 3。** 上一轮整个实验的公平性压在一条纪律上："不许各挑各的阈值，必须在**空样本正确率 = 100%** 这个相同工作点上比 recall@3。" **这条纪律是对的、而且是关键的**（没有它两边各报最好看的点，比较毫无意义）。**但用来锚定工作点的那个指标，当时的分母只有 3，而且那 3 条恰好都是"完全不相关"的容易样本**（几何、钟表、背古诗）—— 一个方法只要能对这三类说"不知道"就拿到 100%，就被判定站在了公平的起跑线上。补到 11 条之后：生产阈值 0.2 的空样本正确率从 100% 掉到 **91%（10/11）**、字面工作点 0.15 → **0.25**、基线 89% → **79%**、向量 0.7 → **0.75**、79% → **53%**。
  - **一句话教训：纪律守住了"不许作弊"，守不住"指标本身没意义"。**
  - **这是"小样本上的百分比要当心"第一次不是发生在被比较的对象身上，而是发生在裁判身上。** 上一轮已经很小心了 —— 对 recall@3 那 19 条反复强调 ±1 条 ≈ ±5.3pp、算了 McNemar、拒绝说"显著优于"。**那把统计谦逊的尺子，一次也没有量到空样本正确率这个指标上。因为它当时不被当成"结果"，而被当成"实验装置的一部分"，而实验装置是不会被审的东西。**
  - **可复用的规则**：(a) 凡是用来定义**工作点 / 闸门 / 判定标准**的指标，必须和被测指标受**同等审查** —— 问同一个问题"它的分母是几？±1 条会让它跳多少？"；(b) 判据类样本必须**覆盖多个互斥类别**，不能全是同一种容易样本；(c) **先定分类、按分类写死样本、写完才第一次跑分数** —— 反过来做就是拿被测系统的输出去设计测它的集合。
- **② 补样本的方法（本轮结果可信的前提）**：先定三类再写死 8 条，写完才第一次跑分数。**其他领域 5 条**（统计与概率 2 / 图形与几何 2 / 综合与实践 1 —— 本图只覆盖"数与代数"）、**完全非数学 2 条**（写作业拖拉、考试紧张）、**太笼统无从诊断 1 条**，加已有 3 条共 11 条。**刻意排除两类**：「领域内但图没覆盖」（百分数/负数/解方程 —— 旁边都有相邻节点，标签本身有争议，会污染指标而不是提高分辨率）与「说的是对的」（那测的是能否分辨对错，是另一个问题）。**代价要记住：这道门槛目前只被"完全不沾边"压过，还没被"沾边但不该召回"压过，而后者才是生产里更常见的一类。**
- **③ "字面基线"被层层更正到第三层，三次变的都是判据不是模型**：84%（阈值 0.2，生产默认值）→ ~~89%（阈值 0.15）~~ → **79%（阈值 0.25）**。字面在 0.25 上的能力和上一轮在 0.25 上**完全一样**（recall 那一列一个数没变），变的只是每个阈值配得上的空样本正确率、也就是**工作点的位置**。上一轮写下的"以后引用基线一律用 89%"**已作废**；"0.15 同时拿到 89% 召回 + 100% 空样本正确率"这句也作废 —— 它当时是对的，但只在分母为 3 的意义上对。**新规矩：引用基线必须连工作点一起写**（"79% @0.25，同工作点口径"／"84% @0.2，生产默认值口径"），**光写一个百分比这条记录已经栽过两次。**
- **④ 向量那条"判别力不够"的论断，现在有了直接得多的证据**：空样本正确率在**余弦 0.55 及以下全是 0%**。上一轮是 0/3，可以辩解成"三条恰好都难"；现在是 **0/11**，分属三个互斥类别 —— **没有一条**能在 0.55 以下被判为不相关。也就是说 bge-m3 对"孩子写作业拖拉，一坐下就要玩橡皮"和一条小学数学误概念照样给出 0.55 以上的余弦。上一轮靠间接推理（平坦段 + `limit=5` 截断导致的非单调跳变）得到的结论，这次是**直接测出来的**。顺带发现：上一轮那条 0%→33%→67%→100% 的"爬升曲线"**形状是被分母 3 量化出来的**，不是模型的性质（真实形状是 0%→9%→64%→91%→100%）。
- **⑤ 一条被数据推翻的"理由"（决定是对的，理由是错的）**：昨天把 `min_relevance` 从 0.15 改回 0.2，写下的理由是"0.15 会多漏空样本"。**在 11 条空样本下这条理由不成立** —— 0.15 与 0.2 的空样本正确率**一样都是 91%**，而 0.15 的 recall 还高 5 个百分点。那条理由只在单测那条 fixture 上成立（"孩子不爱吃青菜"，相关度 0.1667，卡在 0.15 与 0.2 之间，**该样本不在 ground truth 里**）。**但"改回 0.2"这个决定本身仍然是对的**，对的理由是"单测演示了一个真实的误招模式，而当时的 ground truth 分辨率看不见它"。**决定和理由要分开记账，否则下次会拿一条错的理由去推别的结论。**
- **⑥ 于是开放问题换了个形状：不再是 0.15 vs 0.2，而是 0.2 vs 0.25。** 生产默认值 0.2 现在有一条**实测的已知误招**：`empty_bar_chart`（"看条形统计图……读不出具体的数量"）误召「用字母表示关系」的 statement「字母只能代表一个具体的数……」，**相关度 0.2105**，重叠的是"具体的数"这类通用词。0.25 是 100% 空样本正确率的起点，代价 recall 84% → 79%（掉的两条都是**从命中变成空手而归**，不是变成误召）。**没有替它下结论**，权衡写进 backlog。**这次不像上一轮那个 0.15，没有"免费的 5 个百分点"可捡了。**
- **⑦ 诚实性纪律：差距从 10pp 变成 26pp，不构成把话说满的理由。** n 还是 19，±1 条仍值 5.3pp，26pp 只等于 5 条样本，换掉两三条 ground truth 就能变成 15pp 或 35pp。**结论变稳靠的是判据分辨率提高，不是差值变大。** 而且**本轮做不了配对检验**：向量在新工作点 0.75 上的逐条明细没取（要重装 GB 级可选依赖，为保住"零模型依赖"这条性质的干净验证而放弃）。"向量命中集 ⊊ 字面命中集"这条只在**旧那对阈值（字面 0.15 / 向量 0.7）**上验证过 —— 有趣的是**那对阈值在新 ground truth 下恰好仍然互相匹配，只是匹配在 91% 而不是 100%**，所以那条结论原样成立，但**不能外推到 26pp 那个对比上**。
- **⑧ 口径纪律：新数不许悄悄替换旧数。** 建索引耗时本轮实测 **3.2s**（编码 383 条文本），上一轮记的是 **~10.5s**（编码 375 条）—— **两者不是同一件事的两次测量，是两个口径**：3.2s 是模型已在本地缓存的**热启动**值，上一轮那个区间含更多首次开销。文档里两个并列 + 写明口径差异，**引用"建索引要花多久"时 ~10.5s 那档更接近真实首次体验，3.2s 是理想下界**。（383 = 353 条语料 + 30 条观察句，涨的 8 条正是新补的空样本。）
- 产出：`practice/cn-curriculum-graph/docs/rag-vs-literal.md`（新增 §1.1 补样本方法、**§6.0 判据太脆**、§3.1 层层更正、§3.2 生产默认值的已知误招；§2/§3/§4/§5 全部数字重扫）、`docs/rag-vs-literal-backlog.md`（旧的"0.15 vs 0.2 已结案"追加补记 + 新开放问题"0.2 vs 0.25"）、README 第 12 条与结论摘要、`data/diagnosis-eval-groundtruth.json`（22 → 30 条，`_note` 已更新）。**代码零改动**，396 测试全绿。
- **下一步**：
  1. **想把 0.2 vs 0.25 定下来，缺的是"沾边但不该召回"那一类空样本**（本轮刻意排除的两类之一）。但这条建议自己要打折：上一轮说完"缺样本"之后，补样本推翻的东西比预期多 —— **别把"再补一批样本"当成一定能给出答案的动作**。
  2. **向量侧在新工作点的逐条明细还欠着**：`uv sync --extra embed` 后一条命令 `--scorer vector --min-relevance 0.75` 就能补上（能补齐 @1/@5 与配对检验）。在那之前 `rag-vs-literal.md` §4 的向量列只能标着"上一轮 @0.70"读。**注意这会把 `.venv` 从 82M 撑到 825M，破坏"零模型依赖"那条性质的干净验证环境。**
  3. **边质量仍是主要瓶颈**（31 条边 / 26-64 孤立 / 最长链 3 层），规划线不可用 —— 优先级仍在检索调优之上。
  4. 向量缓存 key 未 strip 那条**必须在下一轮开始前改，不能在轮中改**（会动实验条件），见 backlog。

## 2026-07-27（第四段）— 字面 vs 向量受控对比：花 4.3G 买到的是一次对自己诊断的证伪

- **一句话**：真装依赖、真下 `BAAI/bge-m3`、只替换 `_relevance()` 内部跑完受控对比 —— **向量没有赢**。同工作点（空样本正确率 = 100%）上 **字面 89%（阈值 0.15）vs 向量 79%（阈值 0.7）**，且**向量的命中集是字面命中集的真子集，零个独赢样本**。ADR-0006 不推翻，追加补记。产出 `practice/cn-curriculum-graph/docs/rag-vs-literal.md`，393 测试。**[2026-07-28 更新：本条里的每一个基线数字都已被空样本 3 → 11 条的重扫改写（新数是字面 79% @0.25 vs 向量 53% @0.75），方向没变。**结论保留原样不删，作为"判据太脆"那条教训的现场证物** —— 见本文件 2026-07-28 那条。]**
- **① 立项理由本身没站住 —— 这才是本轮最贵也最值的收获**。实验的立项依据是"字面版剩下的 3 条未命中都指向具体数字被换掉后字面就废了，而那正是向量天然该解决的问题"。逐条验完：
  - `compare_only_first_digit`（同义换词）**根本不需要向量** —— 阈值从 0.2 挪到 0.15 就免费召回了。它当初被记成"字面就废"的证据，其实是阈值定高了。
  - `order_of_operations`（`20+5×2` vs 图里 `15+3×4`）与 `dec_more_digits_bigger`（抽象规则 vs 具体数字）—— **向量和字面一起失手**，后者两边还错召同一个节点。
  - 即：被点名"必须上向量"的三条，一条是阈值问题、两条向量也救不回来。**"字面匹配在换数字时会废"这句话听起来太对了，对到没人（包括我）想去验证"那向量就不会废吗"。**
- **② 一条被自己证伪的旧记录**（已就地划掉，见下一条）：上一轮写的"扫过全档阈值确认：单纯调阈值只是沿同一条前沿滑动，换不来净收益"**不成立**。真正的全档扫描里 **0.15 同时拿到 89% 召回 + 100% 空样本正确率**，比生产默认值 0.2（84%）净高 5 个百分点。上一轮大概率只扫了 0.1 和 0.2、跳过 0.15。**元教训：说"扫过全档"之前先看自己扫了哪几个点；"我没扫到"不等于"不存在"。** 顺带：**长期被引用的"字面基线 84%"是错的，真基线是 89%** —— 拿 84% 当基线会让任何新方案凭一个被压低的基准"赢"。（**但没有因此就去改 `MIN_RELEVANCE`**：0.15 是在同一份 22 条 ground truth 上选出来的，用选点的数据验证选点等于没验证；且 5 个百分点只等于 1 条样本。只记录发现。）
- **③ 前沿曲线的形状比端点值信息量大得多**。字面版的动态范围是 0.05–0.80（宽 0.75），recall 平滑下滑 95%→0%；**向量版前 11 个阈值（0.05–0.55）完全平坦** —— recall 恒 89%、空样本正确率恒 0%。平坦意味着在余弦 0.55 以下，完全无关的句子也和数学误概念挤在同一个分数带里。于是它的困境是结构性的：**"敢返回空"要求阈值 ≥0.70，而召回峰值（95%）在 0.60–0.65，两个需求在它的分数分布上重叠了**。**不是"语义理解不够"，是判别力（separability）不够。**
- **④ 向量新掉的两条，掉法很典型**：`frac_unit_compare`（"八分之一比五分之一大"）字面靠 `normalize_math` 改写成 `1/8` 直接命中"分数的意义"，向量召回**"认识成正比的量"** —— dense 检索的经典失败形状：**主题相邻但事实错误**，而且比空手而归更危险（消费端可能当真）。`place_value_ignored`（"数字写在哪一位都无所谓"）字面靠"数位"二字命中，向量空手而归 —— **口语↔术语的对应恰恰是语义模型该做的事，结果字面做到了**。一般观察：**中文数学术语是高度自解释的复合词**（数位/分数/交换律/质数），家长口语里往往原样带着它们；把词形抹掉换成 1024 维稠密向量，在这类语料上是净损失。
- **⑤ 诚实性纪律：不能把"向量输 10 个百分点"讲成碾压**。n=19，±1 条命中 ≈ ±5.3pp；89% vs 79% 只差 2 条样本，配对下不一致样本仅 2 条（McNemar 精确检验 p = 0.5，**统计上完全区分不开**）。所以能说的是：**向量没有赢，而代价是确定且巨大的 → 收益不确定而代价确定时答案是不买。这是决策问题，不是统计问题。** 同一把尺子也用在自己身上：那个"生产阈值让出 5 个百分点"同样只是 1 条样本，所以标成"发现"不是"该改"。
- **⑥ 代价必须实测，而实测总会多出估算不到的账**（四项 + 两笔意外）：模型 **4.3G**（下载前目录不存在）｜`.venv` **81M→825M（+744M，29 个包）**，`uv sync --extra embed` **9.5s**｜建索引 **0.00s→~10.5s**（编码 375 条文本）｜单次查询 **1.6ms→29.7ms（新提问）/12.0ms（复查）**。两笔估算不出来的：**(a)** 4.3G 里有一半是白拿的 —— bge-m3 主分支只有 `pytorch_model.bin`(2.27G)，`transformers` 5.x 会**在后台线程里从 PR 分支 `refs/pr/130` 另抓一份 `model.safetensors`(2.27G)**；**(b)** 那个后台线程是**非 daemon** 的，会挂住进程退出 —— 实测结果全打印完之后卡了 **9 分 50 秒**，`sample` 抓栈显示主线程停在 `Py_FinalizeEx → wait_for_thread_shutdown` 等一个卡在信号量上的 `hf-xet` 线程；诱因是缓存里残留的 `*.incomplete` 分片，清掉后同一条命令 27.1s 干净退出。**"多一个 GB 级依赖"这句话展开之后是这个样子。**
- **⑦ 分层那道边界的最终验收**：回到不带 extra 的环境（`uv sync` 卸掉 `sentence-transformers` 及其 28 个传递依赖，`.venv` 897M→82M），**393 个测试一个不掉**（收尾修复波补了 3 条打分器接线测试后为 396，同样全绿）。领域层全靠假 embedder 跑 —— `Embedder` 协议这道边界唯一要证明的事，证明了。
- **⑧ 方法论沉淀（可复用到任何"要不要上更复杂方案"的场合）**：心法②"尽可能久地保持简单"的操作化版本**不是"不要用复杂方案"，而是"上复杂方案前，先让它和简单方案在同一个工作点上比一次"**。本轮的全部纪律都是为了让这次比较没法作弊、**包括没法被我自己作弊**：同工作点比较（不许一个在 A 点一个在 B 点）、只动一个变量（聚合权重一行不改，否则涨的分说不清归谁）、**字面基线现算不写死且扫描区间与用户的 `--sweep-*` 解耦**（否则用户为余弦收窄区间会把字面基线算低，是比"写死"更隐蔽的自欺）、够不到工作点就返回 `None` 并非零退出（不许退而求其次挑一个召回高的点报）。
- 产出：`docs/rag-vs-literal.md`（六节：设计/完整前沿/同工作点/逐条对照/四项代价/诚实结论）、`scripts/eval_diagnosis.py` 新增 `--min-relevance`（让逐条明细能停在扫描报出的工作点上 —— 否则"扫描说 89%/79%"和"逐条列出的未命中样本"来自两个不同工作点）、`serve/scoring.py` 就地更正被证伪的注释、README「怎么跑」补向量跑法 + 第 12 条结论；393 测试全绿。
- **下一步**：
  1. **边质量仍是主要瓶颈**（31 条边 / 26-64 孤立 / 最长链 3 层），规划线不可用。**把 4.3G 花在检索上、让图继续稀着，是优化错了地方** —— 19 条里字面已命中 17 条。
  2. 若还要推进向量路线，**下一个受控变量是 hybrid（dense + 字面）而不是换模型** —— 因为两者失败模式**不重合**（向量丢的 `place_value_ignored` 字面稳稳命中），融合有理论收益。但守同一条纪律：先在同工作点上量，再决定要不要留。**注意"实验台是现成的"这句话只对换模型成立，对 hybrid 不成立**：排名融合（RRF 之类）需要打分器看到候选集，而现在的 `Scorer.relevance(query, target) -> float` 只看到一对文本；退而求其次的分数融合又撞上"余弦与覆盖率标度不可比、单个 `min_relevance` 闸门服务不了两者"。**做 hybrid 的第一步是决定要不要把这层换成 `Retriever(query, k) -> list[(id, score)]`，而那会重新引出"聚合权重放哪一层"这个本轮受控性的根基问题** —— 工作量不是"接一下"，要按这个量级估。
  5. 收尾 review 分诊出的延后项清单见 `practice/cn-curriculum-graph/docs/rag-vs-literal-backlog.md`。其中**向量缓存 key 未 strip 那条必须在下一轮开始前改，不能在轮中改** —— 它会动到实验条件。
  3. ~~`MIN_RELEVANCE` 0.2 → 0.15 这个 5 个百分点的问题**要动就得先有一批没参与过选点的样本**~~ **[2026-07-28 已结案：真改了一次又改回来]** —— 改成 0.15 后 `test_match_misconceptions_returns_empty_when_nothing_is_close` 立刻变红："孩子不爱吃青菜"命中一条 correction_hint（"引导**孩子**对齐数位…"），相关度 **0.1667**，正好卡在 0.15 与 0.2 之间（7 字 = 6 个 bigram，只"孩子"一个命中，1/6）。**而评测在 0.15 上的空样本正确率仍报 100%** —— 因为 ground truth 里只有 3 条空样本，覆盖不到这个模式。**"前沿上 100%"和"这道门槛守得住"不是一回事，这次实测就是它们分开的地方。** 收益 1 条样本、代价一个被演示出来的误招模式，不划算。要再动，光有"没参与选点的样本"不够，还得有**足够多的空样本**压住这道门槛（现在 22 条里只有 3 条）。 **[2026-07-28 当天就去补了，补完之后这笔账有一半不成立 —— 见本文件 2026-07-28 那条 ⑤/⑥]**：在 11 条空样本下 **0.15 与 0.2 的空样本正确率一样都是 91%**，而 0.15 的 recall 高 5pp，**"0.15 会多漏"这条理由在 ground truth 上不成立**（它只在单测那条不在 ground truth 里的 fixture 上成立）。**但"改回 0.2"这个决定仍然是对的，只是理由要换**；而且 **0.15 和 0.2 都够不到 100% 工作点（那是 0.25），"0.15 vs 0.2"这个问题整个失去了意义。**
  4. 补 `get_next_topics`（暴露 `dependents_of`），排在 1 之后。

## 2026-07-27（第三段）— MCP 暴露层 + 真实对话验收：字段的赌注第一次被兑现/证伪
- **一句话**：六个工具的纯函数领域层 + FastMCP 绑定 + 22 条诊断 ground truth 全部落地（335→363 测试，全程 TDD）。首跑 recall@3 = 63% 闸门红，逐条看未命中后修了**两个实现缺陷**（不是模型问题、不是阈值问题），84% 转绿；MCP server 真机 stdio 握手跑通。**真验收（接进 Claude Code 跑两段对话）尚未做**。
- **① 第一次评测跑出低分时，先确认它在量你以为它在量的东西**。设计文档 §4 预留的分叉是"若明显偏低，结论可能是纯检索不够用 → 回到 §1 论证要不要引 LLM"。真跑出 63% 时，如果照这句话直接去引模型，就会**用一个模型补一个除法写错了分母的窟窿**。逐条看未命中样本才看出掉的是两件蠢事：
  - **长度惩罚**：打分只算"查询被覆盖了多少"，分母是整句。家长转述比图里的误概念长得多，"他说""一样"这类无信息 gram 把真正对上的"交换/减法"稀释到门槛以下 —— **说得越具体越啰嗦，越被惩罚**。改成两个方向的覆盖率取大者（反方向 = 这条误概念的特征被观察句覆盖了多少）。
  - **表示层不同**：图里写 `1/4`、`5-3=3-5`，家长说"八分之一"、"5 减 3"，**字面重叠是零**。不是同义词问题，是同一个数学对象的两种写法；加权和阈值都救不了，归一化两边即可，仍然零模型。
  - 两处修完 63% → 84%，**门槛值一个数没动**。~~扫过全档阈值确认：单纯调阈值只是在"召得回"与"敢返回空"之间沿同一条前沿滑动（89% 召回要拿 0~33% 的空样本正确率去换），换不来净收益。~~ **[已证伪 2026-07-27，见本文件最新一条]**：真正的全档扫描显示**阈值 0.15 同时拿到 89% 召回和 100% 空样本正确率**，比 0.2（84%）净高 5 个百分点。上一轮只扫了 0.1 和 0.2、跳过 0.15，于是把"我没扫到"写成了"不存在"。**教训：声称"扫过全档"之前，先看一眼自己到底扫了哪几个点。**
- **② 一次失败的调优（第二次证明这类记录比成功的值钱）**：针对长度惩罚的第一反应是 IDF 加权，**实测更差（63% → 53%）**。原因反直觉：**IDF 惩罚的是"在图语料里罕见"，而家长口语里的噪声词恰恰在图语料里从没出现过**（df=0 → 权重取最大），把分母顶得更高，方向正好搞反。失败记录已写进 `query.py` 注释，防止第二次尝试。顺带确认 IDF 那层复杂度整体不需要：无 IDF 的目标侧覆盖率同阈值下表现相同甚至更好。
- **③ 一条被数据推翻的设计理由**：设计里定 recall@3 而非 @1，理由是"消费端是 LLM，给它三个候选自己挑"。实测 **recall@1 == @3 == @5 == 84%，三个数完全相同** —— 这个匹配器基本是二值的（要么第一名命中，要么空手而归），**排序维度是空的**，给三个候选和给一个候选没区别。推理本身没错，但它描述的能力当前实现提供不了。要让 @3 真比 @1 有意义，得先有能把"差不多对"的候选排进第二三名的打分函数。
- **④ 评测的命门是 ground truth 本身，所以给它也写了测试**：观察句一律家长口吻改写，并配一条**防抄检查**（观察句与图里 statement/probe 互为子串就红）。抄原文的评测必然高分，而高分什么也不说明。另外钉住：expected id 必须真实存在于图里（打错字会让评测悄悄变成"永远召不回"，症状看起来像检索差）、≥16 条、必须含"应当召回不到"的样本。
- **⑤ 分层的收益在这次是具体的、可预期的**：`serve/query.py` 不 import 任何 mcp 符号，绑定层十几行只转发。MCP SDK v2（FastMCP → MCPServer、协议改无状态）落地时要重写的只有绑定层与它那 10 条测试，领域层与 55 条测试一行不动。这是手写 vs LangGraph 那轮同一手法的第二次应用。
- **⑥ 差点在验收当天才咬人的坑**：默认图路径原本写成相对 cwd，而 **MCP server 的 cwd 由客户端决定**。"在我机器上能跑"会变成"在 Claude Code 里起不来"，且症状只有一句 FileNotFoundError，看不出是 cwd 的事。改成相对包位置解析并补测试。
- 产出：`serve/query.py`、`serve/mcp_server.py`、`scripts/eval_diagnosis.py`、`data/diagnosis-eval-groundtruth.json`(22 条)、`tests/serve/*`、`tests/test_eval_diagnosis.py`、`docs/mcp-server-design.md` §7、工作区根 `.mcp.json`；363 测试。真机验证：`uv run ccg-mcp` 起 stdio server，官方 mcp 客户端完整握手，六个工具全列出，64 节点真实图上返回正确结果且 `provenance.review_status=unreviewed` 如实透出。
- **⑦ 验收已跑完（当天，Claude Code 接 .mcp.json，两段真实对话，逐字段核对见 `docs/mcp-server-design.md` §8）**。判据不是"能调通"，是字段有没有真的进入回答：
  - **`misconceptions` 兑现了承诺，而且是以一种设计时没预料到的方式**：诊断线上命中的是「0.3 比 0.03 小」——与家长说的「0.45 比 0.405 小」**同型不同数**。"识别出这是同一个心智模型"正是 §1 决定不放 LLM 时最担心的能力，结果**由字段本身补上了**：误概念是**有限且可枚举**的，枚举完就不需要语义模型去泛化。这条推广性很强 —— **当某类知识可以穷举时，检索 + 数据 就能替掉一层模型**。
  - **`revisits` 完全没用上**：全图 `revisit_count = 0`，字段设计得再好也没数据。它现在的处境，与 MCP server 立项时 `misconceptions` 的处境一模一样 —— **一个至今未经检验的断言**。
  - `evidence` / `assessment_prompt` / `dependencies.reason` 均原样进入回答；`correction_hint` 只贡献了方向（被 agent 改写成钱币模型）；`standards` 只报编号，**对家长无意义 —— 那个字段的消费者是教研/教师，不是家长**（schema 设计时没分清受众）。`provenance` 让 agent 两次都自动降级措辞、标注未审核。
  - **规划线不可用，但失败得很体面**：分数四则的前置只有一条，通分整条链没连出来，`分数的意义` 是孤儿，agent 只能用自身知识补三步并标注来源。**是数据问题不是工具问题**，而 `get_graph_stats` 先报 26/64 孤立、最长链 3 层，让 agent 一开始就知道打几折 —— "防止在退化的图上假装做规划"那个设计意图，真跑下来是起作用的。
  - **工具面缺一个方向（真跑才发现，设计评审发现不了）**：没有"谁以它为前置"。"他会了 X，接下来学什么"是家长的自然问题，六个工具答不了；领域层已有 `dependents_of`，只是没暴露。设计时只想到往上找前置，没想到往下找后继。
- **下一步（断点清楚）**：
  1. **边质量 —— 现在的主要瓶颈**：31 条边、26/64 孤立、最长链 3 层，跨学段前置（整数→小数→分数→比和比例）没连出来。规划线就卡在这里，涉及 edges 层剪枝策略。
  2. 补 `get_next_topics`（暴露 `dependents_of`）。**排在 1 之后**：同一张稀图上新工具照样大面积返回空。
  3. 检索剩余 3 条未命中都指向"具体数字换掉后字面匹配就废"（`20+5×2` vs 图里 `15+3×4`）。一个**未实测的假设**是把数字掩码成占位符再匹配 —— 按本项目纪律，动手前先花十分钟证伪。
- **已知未修（沿用上一条，均不阻塞）**：①`dropped.json` 跨运行累加从不清空；②`source_span` 被抽取层截窄；③`dedupe` 仍串行；④边质量：31 条边最长链 3 层、疑似同义节点串、跨学段前置没连出来 —— 这条现在有了新证据：`get_graph_stats` 在真实图上报 26 个孤立节点（41%），规划线的天花板就压在这里。

## 2026-07-27（下半场）— 真实课标语料上量：判定档位与并发瓶颈的连环暴露
- **一句话**：拿真实课标原文（28 条）第一次上量，连着挖出两个此前看不见的问题 —— fidelity 二值判定淘汰 62%、边审核串行撞超时；修完节点 27→64、耗时从 49:32 超时崩溃降到 **2:27**。
- **拿数据的过程本身有坑**：教育部官网《义务教育数学课程标准（2022 年版）》PDF 是**扫描件**（Adobe Image Conversion，无文字层），`pdftotext` 提取结果为空 —— 只能逐页视觉读。40MB / 189 页，小学·数与代数【内容要求】在正文 p.17–26。
- **顺手补了自己捅的闸门漏洞**：`.gitignore` 注释写着"仓库公开，不做对外分发"，但 `data/source/example.md`（真实课标条目）一直被 git 跟踪。**自己定的闸门自己漏了**，且是在往里加语料时才发现。已收口成 `data/source/` 整目录忽略。另：说明文件不能放在语料目录里 —— `split_source` 的 `*.md` glob 会把 README 当语料切一遍（实测 17 条噪声）。
- **① 「判定档位数是产品决策」的第二次实证，且证据比第一次硬**：72 个 draft 里 45 个被 fidelity 淘汰（62%），而这 45 条里 **21 条（47%）是分歧淘汰**。分歧的形状才是关键 —— 原文「理解数位的含义」→ 描述列举各数位，flash ✅ / pro ❌；原文「会比较万以内数的大小」→ 描述"从高位到低位逐位比较"，flash ❌ / pro ✅，**方向是反的**。不是两个模型各有稳定立场，是这个判断在二值框架下**没有稳定答案**，模型每次随机塞格子。上次（name/desc judge）靠真实数据反推，这次直接看到两个模型在同一灰区互相打架。
- **② 更根本的矛盾：忠实性与有用性在纲领性文本上直接冲突**。课标条目是纲领性的（"理解数位的含义"六个字），而知识图谱节点必须可教、可测。要求 description 严格忠于 `source_span`，等于要求节点停留在纲领的抽象层级 —— 那样的节点没法用。二值判定表达不了这个冲突，三档（faithful / reasonable_elaboration / fabricated）能。改完误杀 45→0，draft 存活 27→64。
- **③ 一次失败的调优比成功的更值钱**：为压掉 2 条漏报加了规则"多出来的部分若够格当另一条课标条目 → fabricated"，**实测更差（漏报 2→3）** —— 内容缺失的样本反而被放行。已回退并保留失败记录。它暴露出**忠实性有两个正交维度：多写（编造）与少写（缺失）**，三档只覆盖了"多写"。这不是调 prompt 能解决的，是档位维度缺一个。
- **④ 闸门要按代价不对称来定，不是按准确率**：评测 17/20=85%，误杀 0、漏报 2。误杀让图稀疏到没法用（已实测：62% 淘汰→347 条边只活 6 条）；漏报只是让个别编造节点混进一张 `review_status=unreviewed`/`confidence=0.0` 的图 —— 那正是 provenance 诚实性设计要兜的场景。于是退出码改成"误杀非零即红，漏报记基线（=2）、变差才红"。**基线是已知限制的显式记录，不是目标。**
- **⑤ 一次真实超时同时引爆三条只活在文档里的结论**：三档把 draft 救活后，边审核工作量从"几乎没有"（此前 292 条边在审核前就随端点连坐死了）变成 690 次串行调用，撞上 `NodeTimeoutError: Node 'review' exceeded its run timeout of 600.000s`，总耗时 49:32。三条一起兑现：**(a)** `NODE_TIMEOUT` 第一次在真实运行里打响（此前只有人造测试）；**(b)** `RetryPolicy` 重试了 `NodeTimeoutError`（它既不在 `PROGRAMMING_ERRORS` 也不是 `ValueError`），整层重跑 3 次白烧三倍调用 —— I1 注释里"粒度是整层"的第一次真实代价；**(c)** 49:32 ≫ 10 分钟，印证"timeout 只改变返回结果、不提供墙钟上界"。
- **⑥ 一个被混淆多时的概念**：`review_edges` 需要全局 `kept_ids` 才能**开始**，但开始之后每条边的判定彼此独立。**"需要全局视野"阻止的是扇出（Send 到条目级），不是层内并发。** 这两件事此前一直被当成一回事，所以边审核从来没并发过 —— 只是以前它没活干，没人发现。并发化后 49:32 超时 → **2:27**。**推论：并发校准只覆盖扇出点，不等于整条流水线都并发了**（`dedupe` 至今仍串行）。
- **并发化必须守住的三条**（都是差点踩的）：产物顺序须与串行完全一致（两引擎对等性测试逐字节比对落盘产物，完成顺序泄漏进排列会让它变成随机红绿）；异常须在工作线程就地转成返回值（`executor.map` 惰性，一抛出后续结果就取不到，"单条失败不中断整批"会静默失效）；`PROGRAMMING_ERRORS` 须显式还原类型 raise（线程池会包装异常，这道防线在并发路径上否则失效）。
- 产出：`docs/mcp-server-design.md`、`docs/source-corpus.md`、`docs/error-taxonomy.md`、`docs/concurrency-calibration.md`、`pipeline/concurrency.py`、`errors.py`、fidelity 三档 + `data/fidelity-eval-groundtruth.json`(20 条，全部取自真实判定) + `scripts/eval_fidelity.py`、`scripts/calibrate_concurrency.py`；284 测试。全程 TDD。
- **图的现状（如实）**：64 节点 / 31 边 / 41% 孤立 / **最长前置链只有 3 层**，且那条链三个节点全在 G1、名字高度同义 —— 更像同一件事被切成三个节点，不是真实先修链。跨学段的链（整数→小数→分数→比和比例）没连出来。诊断线成立（75 条 misconceptions、126 条 evidence），规划线"能跑但薄"。
- **下一步（断点很清楚，可直接接手）**：MCP server 尚未写一行代码，设计已定稿在 `docs/mcp-server-design.md`，无阻塞项。
  1. 先写 `src/cn_curriculum_graph/serve/query.py`（纯函数领域层，**不 import 任何 mcp 符号**）：`load_graph` + 索引 + 六个工具的实现（`match_misconceptions` / `search_topics` / `get_topic` / `get_prerequisites` / `plan_path` / `get_graph_stats`）。全 TDD，fixture 图必须覆盖：带环、soft 边、revisits、孤儿、同名节点、空图。
  2. `plan_path` 的 `known_ids` 语义已定死：传入 id 视为已掌握，**它自己和它的全部上游前置一并剔除**（设计文档 §3 有论证，别改成只剔除自身）。
  3. 再写 `mcp_server.py`（`mcp==1.28.1` 的 FastMCP，十几行转发）。**v2 稳定版目标日期是 2026-07-28，届时加第二个绑定做对比，领域层与测试一行不动。**
  4. 诊断评测：`data/diagnosis-eval-groundtruth.json` >=16 条 + `scripts/eval_diagnosis.py`，阈值 `recall@3 >= 0.75`。样本要含同义换词、跨年级同主题、以及**应当召回不到任何东西**的样本。
  5. 验收不是测试绿，是接进 Claude Code 真跑两段对话（诊断 + 规划），看 `misconceptions`/`evidence` 有没有真的进入 agent 的回答 —— 产出一段实录 + 诚实评价，包括"某字段其实没被用上"这种结论。
- **已知未修（都不阻塞下一步，但别忘了）**：①`dropped.json` 跨运行累加从不清空，报告系统性虚高；修法要绕开 checkpoint 重入对 append 幂等的依赖，得配测试。②`source_span` 被抽取层截窄，导致 fidelity 拿到残缺上下文（fidelity 评测里那条"计算器"漏报就是它造成的）。③`dedupe` 仍是串行，是下一个并发瓶颈候选。④边质量：31 条边里最长链只有 3 层且疑似同义节点串，跨学段前置没连出来 —— 独立课题，涉及 edges 层剪枝策略。

## 2026-07-27 — 接真模型上量前的三件事：三条原判断全被自己推翻
- **元收获（比三条技术结论都值钱）**：这三件事都是上一轮实验**自己写进待办**的"已知技术债"，条条言之凿凿。真去还债时，**三条的前提全部不成立**。共同的病根：待办是在**读到一句警告 / 想到一个理论风险**的当下写的，没有一条是"实测到症状"之后写的。教训不是"别写待办"，而是**待办要标清"这条是实测出来的还是推断出来的"**——推断类待办在动手前必须先花十分钟证伪，否则会照着一个错误前提去改，而"照错误前提改出来的东西"可能比不改更糟（第 ③ 条就是活例子）。
- **① `ValueError` 不重试的误伤面**：原以为要收窄排除集合。全量走完 13 处抛出点后发现——**误伤面今天是零，但零是"恰好"来的**：靠六层函数 catch 边界的当前形状，不是任何结构约束。而**收窄是错的方向**：可达 `retry_on` 的 `ValueError` 不止空 judges 哨兵，`io.append_drops` 直接在四个挂了 retry_policy 的 Node 体里 `json.loads` dropped.json，`JSONDecodeError`/`UnicodeDecodeError` 都是 `ValueError` 子类且都可达，收窄会把这些**确定性**错误变成可重试的，纯亏。**真正的病根是那五处"模型未调用工具"用错了类型**——那不是值错误，是远端服务的协议违约，与限流/超时同类。修法是把它搬出 `ValueError`（新增 `ToolCallMissingError(RuntimeError)`），于是"`ValueError` = 确定性错误 = 不重试"从**碰巧成立**变成**按构造成立**。
- **② 并发上限校准**：原默认值 8 的注释写着"保守起点"。查官方文档发现 **DeepSeek 不设 RPM/TPM，只设账户级并发连接数 2500(flash)/500(pro)**——8 低了两个数量级，**它保守的是一个根本不紧的约束**。全档爬坡 0 个 429。真正的天花板是 `asyncio.to_thread` 的**默认线程池 `min(32, cpu+4)`**：并发设 32，实测在飞峰值只有 20。**而这道墙是我们自己为了让 `NODE_TIMEOUT` 生效而引入的**（`async def` 必须真 `await`，于是所有阻塞调用被扔进线程池）——超时能力与并发天花板是同一个决定的两面，此前从没被联系起来看过。吞吐拐点正好压在这道墙上（19 → 12.60 req/s；24 → 12.09 且 p95 +47%，纯排队）。默认值改成公式 `min(32, cpu+4)`，而不是抄下当天那台机器上的 19。
- **③ `allowed_msgpack_modules`**：原判断"未注册，升级 langgraph 会让 checkpoint 整个失效"。**两处都不成立**：(a) 框架 compile 时**已经自己**从 state schema 递归推导 allowlist（`_serde.build_serde_allowlist`），11 个自定义类型一个不落——手写一份反而更差，它会和 schema 漂移；(b) 未注册的后果不是"失效"，是**静默把 pydantic 模型降级成裸 dict**（ext hook 拒绝时 `return tup[2]` 返回 kwargs），症状是下游一句 `AttributeError: 'dict' object has no attribute 'draft_id'`——**看起来完全像业务代码的 bug**，排查方向从一开始就是错的。**真正的坑在两个独立开关的组合上**：自动推导被 `if STRICT_MSGPACK_ENABLED:`（环境变量，import 时读死）门控，而"序列化器严不严格"是另一回事；`严格 serde + 未设环境变量` 这一格**比什么都不做更糟**（严格拦截照常生效、推导整个跳过）——这正是我第一版修复踩中的，续跑当场炸。最终不依赖环境变量，自己调那份推导函数显式 `with_allowlist`。
- **顺带发现的空档**：校准出一个默认并发值，才发现 `--engine` 根本没有扇出版选项——`max_concurrency` 此前只有实验脚本和测试碰得到。**一个调好的参数如果没有生产路径能用上，调参就只是自娱自乐。** 已补 `--engine langgraph-fanout` + `--max-concurrency`（配另外两个引擎时当场退出而非静默忽略）。
- 产出：`errors.py`、`docs/error-taxonomy.md`、`docs/concurrency-calibration.md`、`scripts/calibrate_concurrency.py`、`graph.py` 的 `build_checkpoint_serde`/`open_checkpointer`/`apply_state_allowlist`、`tests/pipeline/test_checkpoint_serde.py`、`tests/test_error_taxonomy.py`、`tests/test_calibrate_concurrency.py`、`tests/pipeline/test_run_cli.py`；276 测试（+37）。全程 TDD。真实端到端跑通（fanout + 严格 serde + checkpoint，3 条课标 → 4 节点，55s，0 error）。
- 下一步：**MCP server，把图暴露给 agent** —— 从"造数据"切到"用数据"，反过来验证 schema 里 `misconceptions`/`evidence` 这些字段到底能不能被用起来。

## 2026-07-06 — 支持 Copilot CLI + Claude Code 双工具
- 收获：Claude Code 默认只读 CLAUDE.md（不读 AGENTS.md），Copilot 两者都读。以 AGENTS.md 为单一事实来源，CLAUDE.md 用 @AGENTS.md 导入桥接，避免重复维护。工具专属配置分放 .github/ 与 .claude/。
- 产出：CLAUDE.md、AGENTS.md §6、.gitignore、ADR-0003
- 下一步：手写「模型在循环里用工具」最小骨架。

## 2026-07-06 — 构建有效 Agent 三原则（Anthropic）
- 收获：一手权威源（Barry Zhang & Erik Schluntz）。①不要什么都做成 Agent（workflow vs agent，用复杂度/价值/可行性/错误成本 4 条清单判断；写代码是好场景因为可用单测/CI验证）②尽可能久地保持简单（Agent=模型在循环里用工具，骨架=环境+工具+SystemPrompt，功夫在工具设计+prompt）③像 Agent 一样思考（=上下文工程，用模型 debug 模型）。核心：有廉价验证回路的任务才适合高自主 Agent。已升格为工作区默认心法。
- 产出：docs/effective-agents-principles.md，并在 AGENTS.md/roadmap.md 引用
- 下一步：手写「模型在循环里用工具」最小骨架。

## 2026-07-06 — Agent 架构模式目录
- 收获：架构分三正交维度（A单Agent推理范式/B多Agent拓扑/C编排基础设施）+ 跨维度Router+Skill，不是1→7进化线。核心原则「简单优先」，多Agent默认不用。纠正社区分享的硬伤：ReAct=Reason+Act(非Actor)、LangGraph支持环非DAG、LangGraph≠Blackboard、Airflow/Prefect非agent框架、Router+Skill非AI-coding唯一最佳实践。
- 产出：docs/agent-architecture-patterns.md（对一份社区分享的修正重构）
- 下一步：手写「LLM+工具调用」最小循环。

## 2026-07-06 — LangChain 生态辨析
- 收获：LangChain(组件库)/LangGraph(图状态机编排,生产主推)/LangSmith(闭源观测评估)/Langfuse(开源第三方观测)。前三同公司,Langfuse无关。构建层 vs 观测层两个维度,非替代关系。LangGraph 的 State/Node/Edge/reducer 是核心。
- 产出：docs/langchain-ecosystem.md、glossary 新增 5 条术语
- 下一步：手写「LLM+工具调用」最小循环。

## 2026-07-06 — 职业定位与市场/技术背景分析
- 收获：Agentic AI 岗位近年增长 280%~380%，需求超供给；护城河在工程化+场景落地+架构，"只会调 API"贬值。中国以大厂/传统行业转型为主，全球以金融/医疗/企业+AI 实验室为主，全球薪资绝对值更高。技术上行业回归"单 Agent 简单优先"，多 Agent 仅特定场景；框架首选 LangGraph，关注 MCP、可观测、安全。
- 定位：作为 20 年老兵，主打"工程化+领域+架构"组合，绑定一个垂直领域做端到端项目，而非拼"新"。
- 产出：docs/career-and-market-analysis.md
- 下一步：按 roadmap 阶段一，手写"LLM + 工具调用"最小循环。

## 2026-07-06 — 工作区初始化
- 收获：搭建 AI Agent 学习工作区的基础配置与记忆系统。
- 产出：AGENTS.md、.github/copilot-instructions.md、docs/roadmap.md、memory/*。
- 下一步：从 `docs/roadmap.md` 阶段一开始。

## 2026-07-24 — 课标知识依赖图：schema + CI 校验层（TDD）
- 收获：①"规则能查的"和"查不了的"要分层设计——结构层（环/悬挂/单调性）纯规则即可，Marble 这层做得扎实（0环0悬挂0孤立）；真出问题的是内容层（name与description讲的不是一回事），只能靠语义判断，故把 judge 做成依赖注入（CI 接真 LLM，测试接确定性假判定器）。②"跳过"必须留痕：不传 judge 时产出 CONSISTENCY_SKIPPED 警告而非静默通过，否则"CI绿了"会被读成"全查过了"。③跑真实数据是最好的测试：单测全绿后跑 Marble 1590节点/3221边，当场暴露两个自己的 bug（报告按 code 分组导致 error 被藏进 warning 组；适配器漏映射 standards 致对齐率虚报为 0%），两个都先补失败测试再修。
- 产出：practice/cn-curriculum-graph/（schema + 9 条校验规则 + CLI + Marble 适配器 + 38 测试）
- 下一步：生成流水线（多 agent 抽取课标 → 交叉审核 → 产出「数与代数」首批节点）。法律定性未决前不投入大规模生成。

## 2026-07-24 — Marble os-taxonomy 上游分析（从家庭教育工作区迁入）
- 收获：①**分清"地图"与"导航"**——这类开源教育数据集只放出知识依赖图（地图），learner model / 调度策略 / 生成评测（GPS + 路由 + 播报）全留在闭源侧。缺的那三层恰好就是 agent 要做的事，所以它是很好的实验"环境"而非成品方案。②真正的创新点是**把教研资产翻译成 LLM 的调用协议**：边带 hard/soft + 人可读理由、节点带 evidence criteria + assessment prompt，为 LLM 消费而设计，与 2015 年那批为规则引擎设计的图谱不是一回事。③**DAG 建模是有损的**：不含遗忘、螺旋上升、部分掌握（BKT/DKT/IRT 那一层需真实作答数据，开源不了）——这条催生了本项目的 revisits 螺旋边。④**课标年龄错位是真实差异**：人教版四年级的四则混合运算/两位数除法/三角形内角和/平均数被标为 10-11 岁（英国 Y6），说明人教版早约一年 → 印证进度基元该用年级而非年龄，且跨课标数据上 GRADE_INVERSION 噪声高是预期行为不是 bug。
- 产出：sessions/2026-07-24-marble-upstream-analysis.md（原始会话在非 git 目录 ~/Claude/child_watch_baobao，故迁入本仓库）
- 下一步：同上条——生成流水线；另可选：把 os-taxonomy + 社区 taxonomy-mcp 纳入实验素材，若要做调度实验则需自写 learner model（缺的正是这层）。

## 2026-07-27 — LangGraph 重写编排层 + 手写 vs 框架受控实验（路线图阶段四验收）
- **为什么现在做**：手写版已跑通**且带着真实失败模式**（judge 抛 429 炸全批、中断只能从头烧钱）。空着手写状态机学到的只有 API；带着具体痛点写，才知道 checkpointer 到底解决了什么、代价是什么。
- **做法**：六层纯函数一行不改，A 阶段 `graph.py`（一层一个 Node）与手写版并存且行为对等，B 阶段 `graph_fanout.py` 用 `Send` 扇出到条目级。故障注入靠包裹已有的 `PipelineDeps`（当初为可测性做的 DI 原封不动变成了实验装置）。239 测试。
- **核心结论（全部实测，多数对框架不利）**：①**Node 级 `RetryPolicy` 对「LLM 偶发失败」不可达** —— 六层纯函数已把非程序错误吞成 `DropRecord` 从不上抛；能触发时**粒度是整层**（实测 4 chunk 故障在层末尾 → 该层调用 8 次），**手写版的逐条 try/except 在这个粒度上严格更优**。②**`NODE_TIMEOUT` 原本永远打不响**：`async def` 但函数体不 `await`，同步阻塞占住事件循环，看门狗拿不到调度；改 `await asyncio.to_thread` 后生效，但**只改变返回结果、不提供墙钟上界**（`asyncio.run` 收尾 join 杀不掉的线程，`THREAD_JOIN_TIMEOUT=300`）。③**checkpoint 收益 = 崩溃点上游的全部调用量**，随规模放大（n=3/10/20 净优势 3/14/44）；但整层确定性失败时 `RetryPolicy` 是**纯亏损**（重烧 3 遍），故障注在层中间而非层入口时净优势降到 1/6/26 —— 小规模下两者几乎抵消。④**编排代码量 1.65×（机制）/ 2.3×（含重入）**。⑤唯一低估框架处：`Send` 扇出真并发带来 **8× 墙钟加速**（8.08s→1.05s），fake extractor 瞬时返回把它抹平了；反面是并发无上界 → 真实场景 429 风暴 → 被逐条 try/except 安静吞掉。
- **框架强加了什么**（比"省了什么"更值得记）：checkpoint 重入逼你把持久化做成幂等，**还逼你把整个 state reducer 做成幂等**；msgpack 自定义类型未注册（未来版本强制）；异步传染（为让 timeout 生效四个 Node 必须 async，`SqliteSaver` 换 `AsyncSqliteSaver`，`asyncio.run` 使它不能从任何已有事件循环调用）；`retry_on` 默认会重试 `ValueError`，而项目刻意的"配置错误当场炸"哨兵全用 `ValueError`。
- **最贵的一条教训（关于我们自己）**：一份声称"两个实现行为对等"的笔记，其 14 条对等性测试 + 4 条逐字节比对**全部跑在 `checkpoint_db=None` 下** —— **我们的对等性测试恰好绕开了框架唯一有价值的模式**。最终审查的 Critical（跑完的 thread 再跑 → state 累加 → 产物被摧毁，且这是生产 CLI 默认路径）正是从这个缺口长出来的。
- 产出：`pipeline/graph.py`、`graph_fanout.py`、`faults.py`、`scripts/compare_orchestration.py`、`docs/langgraph-vs-handwritten.md`（三章：对等对比 / 额外解锁 / 什么时候不该用它）。
- 下一步：接真模型上量前必须重评 `ValueError` 不重试的误伤面、并发上限按实际配额校准、注册 `allowed_msgpack_modules`。

## 2026-07-26 — 生成流水线（六层纯 DAG 工作流）+ 多 agent 驱动开发实践
- **产品/工程收获**：①**六层里三层不需要模型**（切分、组装是纯规则，去重是规则配对+LLM 确认），这个比例本身是设计质量信号，对应 effective-agents 心法①"不要什么都做成 Agent"。②**剪枝规则由校验规则反推**：`GRADE_INVERSION` 会拒绝"前置年级晚于后继"，那就不生成这类候选——省调用是次要的（N²→N），主要价值是**生成端不产出校验端注定要拒的东西**，同一套约束写两遍是 bug 温床。③**模型只产出它有资格产出的字段**：`id`/`provenance`/`standard_codes` 由代码填，`provenance.confidence` **恒为 0.0**——没有教研审核时任何非零置信度都是自欺，这正是 Marble 最大的信任缺口。把这条做成**结构约束**（`DraftContent` 类就是给模型的 input_schema，装不下那些字段）而非约定。④**课标编号必须在切分层绑定**：`LOW_STANDARDS_COVERAGE` 是带硬阈值的 ERROR，抽不到编号足以让整批产出被自己的 CI 拒掉；编号在原文里有格式，属纯规则能干的活，交给模型平白引入不确定性。
- **最重要的一条教训 —— 原则会在接缝处被稀释**：六层各自都老实记了 `DropRecord`，`review.py` 甚至为丢边写了完整的 `UNKNOWN_REVIEW_TARGET` 记账，但**编排层一行 dict comprehension 的预过滤让那段代码永不执行**，真正丢边处一声不吭。逐层审查看不见它，因为它不在任何一层里。修法之外更重要的是：**把原则写成跨层守恒断言**（最终产出 + 全部 DropRecord ≡ 全部输入），让"没有静默跳过"从口号变成会失败的测试。
- **测试盲区**：159 个测试里每层的 fake 都给自己喂**本层的理想输入**（extract 的 fake 从不返回倒挂年级，edges 的 fake 从不返回重复边，端到端的 fake 让每个 judge 都点头），**没有一个跑过"部分成功+部分失败"**——而那才是真实运行的常态。四个 Critical 全落在这个盲区。
- **多 agent 驱动开发的实测经验**（8 任务 × [实现 agent + 独立审查 agent]）：①**拦截效果分布很说明问题**：逐任务审查拦下的 6 个缺陷**全是单层内部的**（CRLF 泄漏、恒真断言 `assert schema == schema`、三方同名裸名逃逸、`all([])==True` 零判定器满票、共享 `Provenance` 引用、空产出静默绿灯）；最终全分支审查发现的 4 个 Critical **全在接缝上**。**审查单元 = 任务单元，而缺陷单元 = 接缝**，靠加密审查频次补不上，得换审查单元。②**subagent 的自述必须独立验证，包括它对规格的引用**：两次出现实现者给自己的判断加引号、包装成"brief 明确要求的"，而该句在任何文件里检索不到。技术判断本身都对，是表达层的奖励黑客（给自己的决定找权威背书）。已固定在派活提示里加"凡引号引用必须原文可检索"，审查者每轮实地核查。③**比虚构引用更严重的是"知情不修"**：一次实现者在报告里**准确预判了 Critical**（连触发路径都写对）却只写成待办就提交——虚构引用会被检索抓住，知情不修只会静静躺在报告里。规则应是：报告里出现"bug/缺陷/会崩溃"措辞的，要么当次修掉，要么提交前升级裁决。④**审查者故障时要重派，不要自己顶替**：唯一缺失独立审查的那一层（reviewer 两次基础设施故障，我自己写探针顶上），正是最贵的两个缺陷所在地——自己写的探针只覆盖自己想到的不变量。
- 产出：`practice/cn-curriculum-graph/pipeline/`（六层 + 编排 + `ccg-generate` CLI）、`docs/pipeline-design.md`、`docs/pipeline-implementation-plan.md`；159 测试；已接 deepseek-v4-flash 端到端跑通（3 条课标 → 94 秒 → 4 节点，0 error）。全程 TDD。
- **首次真实运行的发现**：fidelity judge 第一次跑就抓到模型在描述里编造了原文没有的"十进制分数/位值"概念（这一层的存在价值当场兑现）；但**淘汰会制造孤儿而流水线无感知**（基础节点被淘汰，后继静默失去前置）；**同年级互为候选产出双向边**，靠 judge 兜住而非剪枝挡住（剪枝没反推 `CYCLE`）。
- 下一步：接真模型上量前必须先修——剪枝反推 CYCLE、淘汰孤儿感知、`run_all` 误报 `CONSISTENCY_SKIPPED`（review 层明明跑过 name judge）、补重试退避与 `--from` 重入、`candidate_pairs` 的 O(n²)。

## 2026-07-26 — 判定标准从二值扩到三档：档位数是产品决策（TDD）
- 收获：①**判定档位不够时，模型不会告诉你"没有合适的选项"，它会硬塞进现有某一档**。二值 judge 在 8 条手写 ground truth 上 100%，一上 Marble 真实数据就暴露出第三类情形：名称与描述**同属一个主题但覆盖范围对不上**（Deep-Sea Survival 的描述扩到木蛙和水熊虫）。这类被硬塞进"不符"，于是 ERROR 里混进大量本该是 WARNING 的东西。**改成三档判定（consistent / scope_mismatch / topic_mismatch）→ 两级严重性（无 / WARNING / ERROR）后，ERROR 从外推的约 140 条降到真实的 28 条。**②**ground truth 的样本分布决定了你能发现什么**：手写样本全是"乘法 vs 除法"这种泾渭分明的，压根测不出边界在哪；必须拿真实数据反哺 ground truth（扩到 16 条，含 6 个边界案例 + 1 条初版误判的对抗样本）。③**schema 用 Literal 而非 Enum**：pydantic 把 Literal 内联成 `{"enum":[...]}`，Enum 则生成 `$defs` 引用——各家结构化输出/工具 schema 对 `$ref` 的支持参差不齐，内联更安全。④**LLM 生成的知识图谱，同名节点是高危区**：全量 1590 节点跑完 28 条 ERROR 里 **86%(24/28) 落在跨年龄段复用的同名节点**上（`Understanding angles` 一名七用，其中 4 个描述分别在讲长方形面积/四边形分类/尺规作图/长方形性质）。不是随机噪声，是"名称当主题族标签复用、描述来自各年龄段具体课标条目"导致的逐级漂移——本项目的生成流水线要专门防这一手。
- 全量实测（deepseek-v4-flash，10 并发，205s，0 失败，约 $0.08）：consistent 1454(91.4%) / scope_mismatch 108(6.8%) / topic_mismatch 28(1.8%)。16 条 ground truth 上三档查准查全均 100%。
- 产出：`Verdict.judgment` 三值 Literal + `is_consistent`、新增 `NAME_DESC_SCOPE_MISMATCH`(warning)、prompt 教三档区分（含"拿不准倾向 consistent"）、eval_judge 改 3×3 混淆矩阵（漏报/降级才非零退出）、ground truth 8→16 条；58 测试（+3）。
- 下一步：生成流水线骨架（多 agent 抽取→去重→依赖边→交叉审核，先用十几个手写种子跑通链路）。

## 2026-07-26 — 多 provider judge：DeepSeek 接入 + 真实数据实测（TDD）
- 收获：①**结构化输出不是可移植特性，"强制工具调用"才是**。DeepSeek 的 Anthropic 兼容端点（`https://api.deepseek.com/anthropic`）能用同一个 anthropic SDK，但**照收 `output_format` 却不遵守**（实测返回自由文本"否"，SDK 在 JSON 解析上炸）；改成「强制调用一个 `input_schema` 就是 Verdict 的工具」即通 —— 任何支持 tool use 的模型都能跑，代价是自己 `model_validate` 一遍。另：DeepSeek v4 默认开 thinking，而**思考模式不接受强制 tool_choice**（400），需显式 `thinking={"type":"disabled"}`；分类任务本也不需要思考预算。②**judge 多样性必须来自模型，不能来自提示词**：两个 judge 共用同一份系统提示（`judges/prompt.py`），否则分歧分不清是"模型看法不同"还是"问题问得不一样"，投票机制失去意义。而 Anthropic/DeepSeek 是不同训练谱系，误判模式不重叠，正好当交叉审核的独立投票者（同族模型误判高度相关，投两次约等于投一次）。③**干净样本会掩盖定义歧义**：8 条手写 ground truth 上 deepseek-v4-flash 拿了 100%，但抽 Marble 真实节点 124 个跑出 11 条(≈9%)，其中 6 条是「名称与描述**覆盖范围**不一致」而非「讲的是不同知识点」—— ground truth 测不出的语义缺口，真实数据一跑就现形。④**环境变量会互相踩**：DeepSeek 官方教 `export ANTHROPIC_BASE_URL=...`，但同机 Claude Code 也读这个变量，export 出去会把 Claude Code 整个劫持走 —— base_url 一律代码里显式传，key 用独立 `DEEPSEEK_API_KEY`。
- 成本/性能实测：deepseek-v4-flash 单次约 1.4s；8 并发跑 124 节点 22s、0 失败。全量 1590 节点约 $0.08（对比 Haiku 约 $0.6）。订阅走 `claude -p` 也能当 judge 且判得对，但单次 26s、烧 26k token（有效输入只有 10）—— agent runtime 的固定开销，批处理场景不成立。
- 产出：`judges/deepseek_judge.py` + `judges/prompt.py`（抽出共用提示）+ CLI `--judge {none,anthropic,deepseek}`（每个 judge 认自己的 key，缺 key 清晰报错退出 2）+ `eval_judge.py --judge`；55 测试（+11）。全程 TDD。
- 下一步：**先定死"范围不匹配算不算名实不符"这条产品语义**（本项目第一个非工程决策），把那 6 个边界案例补进 ground truth 并据此调 prompt；然后才是生成流水线骨架。

## 2026-07-24 — cn-curriculum-graph 接真 LLM judge，激活 NAME_DESC_MISMATCH（TDD）
- 收获：①**judge = 纯函数 (name, description) -> Verdict，Protocol 钉死契约，LLM 只是一个实现**。project 里第一个"模型在循环里"，也是生成流水线"交叉审核"层的前置（那层本质是多 judge 投票）。②**结构化输出用 messages.parse(output_format=Verdict)**，逼模型直接返回 {consistent, reason}，Verdict 的 extra="forbid" 恰好满足结构化输出要求的 additionalProperties:false；temperature=0 求可复现；分类任务默认 Haiku 4.5（便宜、够用，构造参数可覆盖，评测证明不够再升）。③**client 依赖注入是可测性的关键**：构造时可注入 fake client → 单元测试验"接线"（喂对参数、翻译对返回）而零网络零 key；anthropic 懒加载，测试连 SDK 都不碰。④**有 ground truth 才敢信 LLM 判定**：拿手工核对的 4 个 Marble 名实不符节点当标尺，eval_judge.py 算 accuracy/precision/recall，漏报(FN)非零退出——这套 ground truth 基建将来复用给流水线审核层。
- 产出：judges/anthropic_judge.py + scripts/eval_judge.py + data/judge-eval-groundtruth.json + CLI `--judge anthropic`/`--model`；44 测试（+6）。全程 TDD（先写失败测试再实现）。anthropic>=0.69 入 deps。
- 待办：本地拿真 ANTHROPIC_API_KEY 跑 eval_judge.py 量 Haiku 准不准（无 key 时清晰报错退出 2，非 traceback）。
- 下一步：生成流水线骨架（多 agent 抽取课标→去重→依赖边生成→交叉审核，先用十几个手写种子跑通链路）。法律定性只卡"大规模生成+对外发布"，本地自用不受阻。
